[project]
name = "image-stitching"
version = "0.1.0"
description = "Multi-image stitching under translation / similarity / affine / homography motion models"
readme = "README.md"
requires-python = ">=3.10"
dependencies = [
    "numpy>=1.24",
    "opencv-python>=4.10",
    "matplotlib>=3.7",
]

[tool.uv]
package = false
